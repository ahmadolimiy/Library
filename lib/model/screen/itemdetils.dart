import 'package:flutter/material.dart';





class Itemetials extends StatelessWidget {
  const Itemetials({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ccc'),),
      body: Column (
        children: [
          Text('ff') ,
      //    Image.memory(null) ,
Text('dddd'),



        ],
      ),


    );
  }
}
